import WormholeBridge from "@wormhole-foundation/wormhole-connect";

const Bridge = () => {
  return (
    <main className="z-40 w-full h-full min-h-screen text-white">
      <WormholeBridge />
    </main>
  );
};

export default Bridge;
